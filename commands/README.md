# TIDOS Session Commands (`commands/`)

The `commands/` directory contains protected session command definitions and invocation protocols for TIDOS (v3.0).

---

## Directory Components

- [session_commands.md](file:///d:/work/Dev/TIDOS/commands/session_commands.md): Standard session invocation commands, parameters, and expected agent responses.

---

## Protection Rule

```
INVARIANT: All files in commands/ are PROTECTED system files and read-only.
```
